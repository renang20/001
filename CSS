body a {
    text-decoration: none; /* Remove o sublinhado */
    color: #ffffff; /* Define a cor do link */
  }
  
  /* Estilize todos os links quando o mouse passa por cima deles */
  body a:hover {
    text-decoration: underline; /* Adicione um sublinhado quando o mouse passar por cima */
  }
body, header, nav, ul, li, p, h1, h2, h3, a, div, rodape {
    margin: 0;
    padding: 0;
}

/* Estilos globais */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    align-items: center;
}
h1{
    display: flex;
    align-items: center;
    background-color: black
    
}
header {
    display: flex;
    align-items: center;
    background-color: rgb(0, 0, 0);
    color:blockcenter;
    padding: 
    
}

nav {
    background-color: rgba(0, 0, 0, 0.5);
    padding: 1em;
    display: flex;
    justify-content: center;
    position: sticky;
    top: 0;
    z-index: 100;
}

nav a {
    color: white;
    text-decoration: none;
    margin: 0 1.5em;
    transition: color 0.3s;
    cursor: pointer;
}

nav a:hover {
    color: #ffffff;
}

/* Estilos da barra de pesquisa */
.search-container {
    display: flex;
    align-items: center;
    border: 1px;
    
}
.botao{
    display: flex;
    margin-left: 750px;
}
.search-box {
    padding: 10px;
    border: none;
    border-radius: 5px 0 0 5px;
    order: 2;
}

.custom-search-button {
    background-color: black;
    color: white;
    border: none;
    border-radius: 0 5px 5px 0;
    padding: 10px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s;
    display: flex;
    align-items: center;
    order: 1;
}

.custom-search-button i {
    margin-right: 5px;
}

.small-icon {
    width: 20px;
    height: 20px;
    margin-left: 5px;
}
/* Adicione estilos aos slides e ao layout de notícias */
.slide-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0px;
    background-color: #030303;
  }
  
  .slide-content img {
    max-width: 45%;
    height: auto;
    margin-right: 0px;
  }
  
  .news {
    color: #f1eae5;
    flex-grow: 1;
  }
  
  .news h2 {
    color: #f1eae5;
    font-size: 20px;
    margin-bottom:0px;
  }
  
  /* Restante do seu estilo CSS para o carrossel... */
  
/* Estilos para os cards de notícias */
.news-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    padding: 3em;
}

.news-card {
    width: calc(33.33% - 40px);
    margin-bottom: 20px;
    background-color: rgba(0, 0, 0, 0.7);
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    overflow: hidden;
    transition: transform 0.3s, box-shadow 0.3s;
}

.news-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(6, 6, 6, 0.3);
}

.news-thumbnail {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.news-content {
    padding: 1.5em;
}

.news-title {
    color: white;
    font-size: 18px;
    margin-bottom: 10px;
}

.news-excerpt {
    color: #ccc;
    font-size: 14px;
}
.rodape{
    text-align: center;
    background-color: black;
    color: #f1eae5;
    display: block;

}
.icones :hover{
    color: #fffdfc;
    transform: translateY(-5px);
    transition: transform 0.3s, box-shadow 0.3s;
}
